name: sevasetu
description: "SevaSetu — Connecting NGOs, Surveyors, and Volunteers for community relief across India."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  dio: ^5.7.0
  shared_preferences: ^2.3.4
  fl_chart: ^0.70.2
  intl: ^0.19.0
  image_picker: ^1.1.2
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0

  # Phase 2 — Firebase (requires flutterfire configure)
  # firebase_core: ^3.8.0
  # cloud_firestore: ^5.6.0
  # firebase_auth: ^5.4.0
  # firebase_messaging: ^15.2.0
  # firebase_storage: ^12.4.0

  # Phase 2 — Native setup required (API keys, permissions)
  # google_maps_flutter: ^2.10.0
  # speech_to_text: ^7.0.0
  # google_generative_ai: ^0.4.7
  # flutter_image_compress: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
